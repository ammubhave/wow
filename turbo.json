{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": ["WRANGLER_SEND_METRICS"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  },
  "ui": "tui"
}
